using Terraria.ModLoader;
using Terraria;
using FargowiltasSouls.Content.Bosses.MutantBoss;
using FargowiltasSouls.Content.Bosses.AbomBoss;

namespace FargosPhantasmMode.Content.SceneEffect
{
    public class MutantP2MusicScene : ModSceneEffect
    {
        /*
        public override int Music => MusicLoader.GetMusicSlot("FargosPhantasmMode/Assets/Music/HeartofGuardian");

        // 优先级设为 BossLow 或更高，确保能盖过普通环境音乐
        public override SceneEffectPriority Priority => SceneEffectPriority.BossHigh;

        public override bool IsSceneEffectActive(Player player)
        {
            // 检查世界中是否存在你的 Boss
            for (int i = 0; i < Main.maxNPCs; i++)
            {
                NPC npc = Main.npc[i];
                if (npc.active && npc.type == ModContent.NPCType<MutantBoss>() && npc.ai[0] > 10)
                {
                    if (npc.ai[0] >= 10)
                    {

                    }
                    if (npc.ai[0] < 0)
                    {

                    }
                    return true;
                }
                    
            }
            return false;
        }
        */
    }
}
